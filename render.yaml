services:
  - type: web
    name: calorie-lens
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false
